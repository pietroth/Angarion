package br.angarion.dev.engine.network.client;

public interface Client {
    /*
        We will not maintain the IDs within the client itself;
        ID management belongs to a structure dedicated to that purpose, such as the ClientLCManager.
        ---
        int getId() throws IllegalAccessException;
        void setId(int id);
    */
}
